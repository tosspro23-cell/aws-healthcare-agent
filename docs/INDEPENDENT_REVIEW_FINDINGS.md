# Independent review findings

Per the challenge brief's own process checklist ("get a second,
independent AI session, not the one that wrote the code, to critically
review the phase before moving on") -- a step every phase in
[`AWS_ROADMAP.md`](AWS_ROADMAP.md) had listed but none had actually done
until now. An independent model reviewed this repository's code (commit
`36a48a8`), with explicit instructions to verify claims against the
implementation rather than take the documentation's self-assessment at
face value.

This file is the full findings list with disposition. Every "Fixed"
finding was independently reproduced against this repo's own code (or,
for the harness-measurement findings, against direct API/tool behavior)
*before* being fixed, and re-verified afterward -- against real deployed
AWS resources wherever the finding was about deployed behavior, not just
against moto/unit tests. See the exact reproduction and verification
steps in [`DECISIONS.md`](DECISIONS.md), which has the full ADR-style
reasoning for each. This file is the tracker.

There have now been three independent review passes: the first against
commit `36a48a8` (below), a second, verification-focused pass against
the fixes for the first (commit `3985ce4`) -- deliberately asked to check
whether the round-1 fixes actually closed what they claimed, not to
re-scan from scratch -- and a third against commit `dd85976`, scoped to
what the first two never touched at all: the entire frontend and the
newly-public S3+CloudFront hosting. **The second review found that
several round-1 "fixes" introduced real regressions** (most seriously:
three ordinary, previously-safe questions -- an LDL trend, an HbA1c
trend, and an eGFR lookup -- started failing the safety check entirely
because of the exact value-unit-binding logic meant to make grounding
*stricter*). **The third review found a live hotfix made the same day
had reopened a genuine safety bypass** (a `question_text` exemption
meant to fix a narrow false positive let a fabricated number matching
one in the question pass as grounded) alongside four real frontend bugs
and one incomplete exception-handling fix. See "Second independent
review" and "Third independent review" below for the full lists and
disposition.

Severity labels are the reviewer's own.

## First independent review (commit `36a48a8`)

| # | Severity | Finding | Fix | Live-verified? |
|---|---|---|---|---|
| 1 | High | Any authenticated caller could read or cancel any other caller's run by `run_id` alone -- no ownership check existed anywhere. | `auth_context.py` + `owner_sub` on every record, enforced atomically in `get_run.py`/`cancel_run.py`. | Yes -- a fabricated second caller gets 404 against a real deployed run they don't own, on both the sync and Step Functions async paths; the real owner still gets 200. |
| 2 | High | `/ask`, `/runs`, and `/jobs` share one `run_id` keyspace with almost no conditional writes -- cross-path overwrites, SQS redelivery reopening completed jobs, cancellation silently undone. | Conditional `attribute_not_exists` on every record's creation; `process_job.py`'s writes conditioned on current status; `cancel_run.py` only attempts `stop_execution` for `execution_type == "STEP_FUNCTIONS"`. | Yes -- a cancelled SQS job stays cancelled after the consumer would have processed it, on the real deployed queue. |
| 3 | High | `enqueue_job.py`'s DynamoDB write and SQS `send_message` are non-atomic -- a failed send after a successful write leaves a `QUEUED` record orphaned forever, with no reconciliation path. | A `send_message` failure now gets a compensating write (`FAILED`, with the real error message) instead of silently orphaning the record. **Not fully closed**: a Lambda crash *between* the two calls (as opposed to a `send_message` call that raises synchronously) is still unhandled -- that needs a real outbox/reconciliation pattern, not a try/except, and wasn't built. | Tested via moto (a forced `send_message` exception); not separately live-tested (the failure mode is a synchronous-call exception, which behaves identically whether mocked or real). |
| 4 | High | Six constructed narrator outputs passed `safety.run_safety_checks` as `safe=True` despite being ungrounded, mis-attributed, or containing dosing/diagnosis language in unmatched phrasings; an empty answer also passed trivially. | `GroundedFact` gained a `unit` field (populated from the source biomarker, not parsed from free text) for value+unit pair verification; the ordinal-list exemption is now position-based, not value-based; the number-extraction regex bug is fixed; diagnosis/dosing pattern lists expanded; added a non-empty check. | Verified against the real pipeline (`HealthAgent.ask()`'s actual grounded facts) -- all six original probes now correctly fail. Not separately re-tested against a live Bedrock call (the fix is in the deterministic verifier, which runs identically regardless of which narrator produced the text). |
| 5 | Medium | `reasoning.py`'s pacing/nutrition branches asserted *both* trigger conditions were reported when the branch only requires *either* one; generic medication/allergy caution categories got converted into specific claims (e.g. levothyroxine, shellfish) regardless of whether that specific medication/allergy was actually what triggered the caution. | Claims are now built from only the signal(s) that actually triggered. The medication/allergy caution now requires the *specific* name (checked against the caution's own `detail` text or the structured profile) to appear before making that specific claim -- a future unrelated caution now correctly produces no claim (silence) rather than a wrong one. | N/A -- pure kernel logic, no deployed behavior to verify beyond the unit tests (which reproduce the exact scenarios: a questionnaire reporting only one signal, and an unrelated-medication caution). |
| 6 | High (for the comparison deliverable) | `stress_test.py --no-retry` didn't actually disable SDK retries (`max_attempts: 1` resolves to `total_max_attempts: 2`, confirmed directly against `client.meta.config`). | Fixed to `total_max_attempts=1`. | Re-measured live: 10/15 (matches the originally published number by coincidence -- the conclusion doesn't change, but the number is now measured correctly). |
| 7 | High (for the comparison deliverable) | `burst-async`'s success check used the raw Step Functions execution status, which reports `SUCCEEDED` even when `RecordFailure`/`RecordTimeout` gracefully caught a real agent failure (both are plain `End: true` states, not an execution-level error) -- a different, laxer success definition than `burst-queue`, which already checked the DynamoDB application-level status. The adversarial sweep also counted HTTP 200 alone, not `safe=True`. | `burst-async` (`_start_and_poll_execution`) now checks the DynamoDB record's status, matching `burst-queue`. `burst-sync`/`adversarial` (`_invoke_ask_handler`) now require `safe=True` in addition to HTTP 200. **Not done**: `race`'s consistency check and the overall reporting still don't split out transport/workflow/application/safety-level outcomes as separate dimensions -- this fix unifies the "did it succeed" question, not the full reporting granularity the finding also suggested. | Re-run live (`burst-async -n 5`): 5/5, DynamoDB-sourced `narrator_backend`/`safe` fields now populate correctly in the harness's own output. |
| 8 | Medium (documentation accuracy) | `STRESS_TEST.md` described SQS buffering as providing "guaranteed eventual success at unlimited scale," overstating what finite retries/retention/DLQ/shared-capacity actually provide. | Reworded to describe what was measured (100/100 at the largest tested burst) rather than an unconditional guarantee. | N/A -- documentation-only. |
| 9 | Medium | The "3 retry attempts" description of the Step Functions retry policy didn't account for CDK's own default retry policy (6 attempts) sitting ahead of the custom one in the ASL for a different, overlapping set of error codes. | Docstrings and docs corrected to describe both policies and how Step Functions resolves the overlap (first matching `ErrorEquals` entry wins); added a test asserting `Lambda.TooManyRequestsException` -- the only error type this project has actually observed -- appears in exactly one of the two policies. | Confirmed via the real synthesized ASL (not assumed); the previously-published throttling numbers are unaffected. |
| 10 | Medium | `narrator_backend` stayed as the *originally selected* backend even after a safety-check fallback to the mock narrator (e.g. still read `"bedrock"` when the mock's output was actually returned); the Step Functions path dropped `narrator_backend` entirely before `record_result.py` could ever persist it (a `ResultSelector` only extracted `answer`/`safe`). | `agent.py` now corrects `trace.narrator_backend` to `"mock"` on fallback. `orchestration_stack.py`'s `ResultSelector` and `RecordSuccess`'s payload now carry `narrator_backend` through; `record_result.py` persists it. | Yes -- re-deployed and re-ran a live async execution: the DynamoDB record now correctly shows `narrator_backend: "bedrock"` (previously would have shown nothing, since the field never reached this Lambda at all). |
| 11 | Medium | `AskHandler`'s IAM grants (`grant_read_write_data`, `grant_read_write` on S3) exceeded what `adapter.py` actually does (only ever writes, never reads a run record or evidence object back). The wildcard-resource test only rejected a bare `Resource: "*"`, missing a wildcard hidden inside a `Resource` list or an action-level wildcard (`"Action": "s3:*"`). | Narrowed to `grant_write_data`/`grant_put`. The shared `iam_assertions.py` helper (used by all three stack test files) now checks every element of a `Resource`/`Action` list, and flags action-level wildcards too -- while still correctly *not* flagging a legitimate bucket-object-suffix wildcard (`bucket-name/*`) or the AWS-managed `AWSLambdaBasicExecutionRole` (a managed policy, not a policy this project's own code creates). | Yes -- a real `/ask` call still succeeds end-to-end (200) after the grants were narrowed, confirming the handler never actually needed the broader permissions. |
| 12 | Medium | The ADR justifying ID-token-over-access-token rested on an incorrect premise: API Gateway's JWT authorizer does support validating against `client_id` when `aud` is absent -- exactly an access token's shape -- so access tokens were never actually incompatible with this project's authorizer config. | `get_dev_token.py` now exports the access token (`CARE_AGENT_ACCESS_TOKEN`). **No CDK/authorizer change was needed** -- the existing `jwt_audience` config already supports both token shapes. **Not done**: real per-route OAuth scopes (a Cognito custom resource server, `authorizationScopes` on each route) -- today any authenticated caller, regardless of token type, can call any route; ownership is enforced at the data layer (finding #1's fix), not via scopes. | **Not yet** -- the actual login flow needs a human at a real browser (the established pattern for this whole project's Cognito flow); whoever next runs `get_dev_token.py` should confirm the resulting access token is accepted end-to-end. |
| 13 | Medium | `start_run.py`/`adapter.py`/`enqueue_job.py` had no validation on `run_id`'s character set or length (only relevant to Step Functions execution names, but all three paths share the same `run_id` keyspace); `ExecutionAlreadyExists` was always treated as a harmless retry regardless of whether the request's input actually matched, and always reported "RUNNING" even for an already-finished execution. | New `run_id_validation.py` (Step Functions' own execution-name character/length rules), applied consistently across all three creation points. `start_run.py` now compares the existing execution's real input against the new request's, reporting a 409 conflict for a genuine mismatch and the execution's *real* current status either way. | Yes -- an invalid `run_id` (embedded space) against the real deployed `AskHandler` returns a clean 400. |
| 14 | Medium | `test_live_endpoint_smoke.py` asserted the deployed backend was `mock` and matched a local run byte-for-byte -- true before Phase 4, stale ever since the deployed default became Bedrock. The retry-policy test only checked the custom policy's presence, not that it actually governs (see finding #9). The stack-dependency test built its own stacks and called `add_stack_dependency` itself, so it couldn't have caught that call being accidentally removed from `app.py`. | Rewrote the smoke test to assert what's actually true of the current deployed system (safe, grounded, backend-agnostic) instead of exact-match text. Added a precedence-specific retry test. Refactored `app.py` into an importable `build_app()` so a test can exercise its *real* stack-wiring code, including the dependency calls, instead of a parallel reconstruction. | N/A for the two test-only fixes; `build_app()`'s refactor was confirmed not to change `cdk synth`'s output. |
| 15 | Low | The real AWS account ID was committed as a hardcoded literal in `auth_stack.py`'s default Cognito domain prefix. | Replaced with `self.account` (a CDK token resolved to the real deploying account at synth/deploy time) -- confirmed via synthesized CloudFormation to resolve to the identical value for a real deploy, so this is a source-code portability/disclosure fix with no behavior change to the live resource. | Yes -- `cdk deploy` for `CareAgentAuthStack` reported "no changes," confirming the resolved domain prefix is unchanged for this account. |

## First review: what it said to keep as-is

Quoted from the review, not re-verified independently here but consistent
with what this project already believed about its own design: the
reasoning/narration separation and optional deterministic path; the
five-stack decomposition (the coupling problem was shared mutable state,
now fixed, not stack count); BM25 and read-only parameterized SQLite
access; the S3/SQS storage baseline (encryption, public-access blocking,
TLS enforcement); conditional terminal writes in the Step Functions path
(`record_result.py`) as the pattern worth extending everywhere, which
finding #2's fix now does across all three paths; and the
experiment-driven approach to finding real throttling/concurrency
behavior in general.

## Second independent review (reviewed commit `3985ce4`, the round-1 fixes)

Deliberately scoped as a *verification* pass, not a from-scratch re-scan:
for each of the 13 findings marked fixed above, does the fix actually
close the gap, or only the specific reproduction originally reported --
and did fixing it break something else? It also looked at areas round 1
didn't touch (BM25/SQLite, the S3 evidence path, Cognito config,
`reasoning.py` more broadly). Every finding below was independently
reproduced against this repo's own code *before* being fixed here
(several live, against the actual mock-narrator pipeline or the real
account's synthesized IAM), the same standard applied to round 1.

| # | Category | Severity | Finding | Fix | Verified? |
|---|---|---|---|---|---|
| 1 | Regression | High | The value+unit binding added to close finding #4 broke ordinary, previously-safe answers: a trend fact's `unit` was never populated (only its numeric value), so any trend answer for a marker not *also* independently flagged as a "focus item" failed grounding purely because the value's only source lacked a unit tag. Separately, a unit string that itself contains digits (eGFR's `mL/min/1.73m2`) had those embedded digits re-discovered as a second, unrelated "ungrounded number" by the fallback bare-number scan. Reproduced live against this project's own shipped sample data: `Is my LDL getting worse?`, `Is my HbA1c getting worse?`, and `What is my eGFR?` all returned `safe=False` at commit `3985ce4`. | `agent.py`'s trend `GroundedFact`s now carry `unit=trend.unit` (the data was already computed by `trend.py`, just never threaded through). `safety.py`'s consumed-span tracking now excludes the *entire* value+unit match, not just the value's own span, so digits embedded in a unit string are no longer independently re-scanned. | Yes -- re-ran all three questions live after the fix; all now return `safe=True`. New regression tests: `tests/test_agent_edge_cases.py::test_trend_answer_for_a_non_focus_marker_is_safe`, `tests/test_safety.py::test_numeric_grounding_does_not_flag_digits_embedded_in_the_unit_itself`. |
| 2 | Regression | Medium | A synchronous `/ask` run (`execution_type=SYNC`) could be "cancelled" (the conditional update only checked owner+status, not execution type) and reported `200 CANCELLED` -- but `adapter.py`'s own terminal write is unconditional, so the moment the in-flight agent call finished, the `CANCELLED` record was silently overwritten back to `SUCCEEDED`/`FAILED`. There is also no execution to actually stop for a synchronous call in the first place. | `cancel_run.py`'s condition now excludes `execution_type = "SYNC"` outright; attempting to cancel one returns `409` with an explicit "cannot be cancelled" message instead of a cancellation that silently doesn't stick. | Yes -- new test `test_orchestration_lambdas.py::test_cancel_run_refuses_to_cancel_a_synchronous_ask_run` reproduces the race and confirms the record stays `RUNNING`, then a `409`. |
| 3 | Regression | Medium | `start_run.py`'s `ExecutionAlreadyExists` handling (added for finding #13) calls `DescribeExecution`, but `StartRunHandler`'s IAM role only ever had `grant_start_execution` (`StartExecution` only) -- a real deployment would `AccessDenied` on every duplicate `run_id` submission. Undetected by moto-mocked lambda tests, which don't enforce IAM. | `orchestration_stack.py` now also grants `states:DescribeExecution` to `start_run_handler`. | Yes, at the infra level -- new test `test_orchestration_stack.py::test_start_run_handler_can_describe_executions` asserts the grant against the *specific* role attached to `StartRunHandler` (not "some policy somewhere"), via the real synthesized CloudFormation template. Not re-verified against the live account in this pass (no new `cdk deploy` was run for this round; the synthesized-template check is the same rigor finding #9's retry-precedence test already used). |
| 4 | Regression | Medium | The compensating write added for finding #3 (mark a job `FAILED` when `send_message` raises) was unconditional -- but an SDK exception doesn't prove SQS rejected the message; it can mean the send succeeded and only the *response* was lost, in which case a consumer could already be processing or have finished the job. The unconditional write would clobber that real outcome back to `FAILED`. | The compensating write is now conditioned on the record still being `QUEUED`; if something else already resolved the run, this write loses the race and does nothing, which is correct. | Yes -- new test `test_queue_lambdas.py::test_enqueue_compensating_write_does_not_clobber_a_job_a_consumer_already_finished` simulates a consumer finishing the job inside the `send_message` exception path and confirms the `SUCCEEDED` record survives. |
| 5 | Regression | Medium | `adapter.py` marked the DynamoDB record `SUCCEEDED` *before* writing the S3 evidence object, with no handling if that write then failed -- leaving a record permanently claiming success with no evidence ever written, and (because the `run_id` is already claimed by the conditional-create guard) not retryable under the same `run_id`. | Evidence is now written *before* the DynamoDB record is marked `SUCCEEDED`; if it fails, the record is marked `FAILED` with the real error instead. **Residual, smaller gap, not fully closed**: the same `run_id` still can't be retried after this (the conditional-create guard still blocks it) -- the caller has to submit with a new `run_id`. Closing that fully would need the same kind of outbox/retry-of-partial-work pattern as finding #3's acknowledged gap. | Yes -- new test `test_adapter.py::test_s3_write_failure_marks_the_run_failed_not_falsely_succeeded` forces a simulated S3 failure and confirms the record is honestly `FAILED`, not `SUCCEEDED` with missing evidence. |
| 6 | Incomplete fix | High | Finding #4's value+unit binding checks that *some* fact carries the exact (value, unit) pair in the text -- not that the text's claimed marker (e.g. "LDL-C") is the one that actually has it. Two markers sharing a unit (common: LDL/HDL/triglycerides/total cholesterol are all `mg/dL`) can still be swapped without detection (e.g. "Your LDL-C is 188 mg/dL" passes even when 188 is only ever grounded as triglycerides). A negative sign is also silently dropped (`"-162 mg/dL"` matches a grounded `162`). | **Not fixed in this pass.** Closing this needs structured claim rendering -- binding concept + value + unit + measurement date together and validating/rendering *those* structured claims, not a regex patch over free text. Documented here as a deliberately open backlog item, the same way finding #3's crash-between-calls gap is. **Closed later** (2026-09-05, without the full structured-claim rewrite): `GroundedFact` gained an optional `display_name`, populated at every real construction site; when set, `verify_numeric_grounding` requires that exact name to appear in a window of text around the matched value+unit, not just exist somewhere among the grounded facts. The negative-sign gap was fixed in the same pass (`-?` added to both numeric regexes). See `docs/DECISIONS.md`. | Reproduced directly against `verify_numeric_grounding` (not just asserted): `tests/test_safety.py::test_numeric_grounding_still_does_not_bind_value_unit_pairs_to_a_specific_marker` documents the gap explicitly (asserts the current, undesired `passed is True`) so it's visible in the test suite rather than silently unrecorded. **Re-verified on close**: the same scenario now asserts `passed is False` (`test_numeric_grounding_closes_the_cross_marker_binding_gap`), a companion test confirms the correctly-attributed case still passes, and a fact with no `display_name` keeps the old permissive behavior (no new false positives for facts this fix doesn't apply to). |
| 7 | Incomplete fix | Medium | Finding #5's fix corrected the pacing/nutrition modifiers' `GroundedFact.claim` (metadata) to reflect only the triggered signal, but left `text` -- what the narrator actually renders into the visible answer -- unconditionally naming both signals. A questionnaire reporting only high stress still produced a `safe=True` answer stating "given reported short sleep and high stress." Separately, the medication/allergy cautions treated a bare substring match as positive evidence, so "Patient denies levothyroxine use" still generated an affirmative claim. | `text` is now built from the same triggered-signal logic as `claim`. A coarse negation-cue check (`denies`, `not`, `without`, etc.) now suppresses the medication/allergy claim when the caution's own detail is a denial rather than a report. | Kernel-level; new/extended tests: `tests/test_reasoning.py::test_pacing_modifier_claims_only_the_signal_that_actually_triggered` and `test_nutrition_modifier_...` now also assert on `.text`, not just `.grounded_fact.claim`; new `test_supplement_caution_does_not_claim_medication_use_from_a_denial`. |
| 8 | New | Medium | The `exercise_limitation` and `family_history_context` modifiers hardcoded a specific claim ("knee pain with running/jumping", "first-degree family history of type 2 diabetes") regardless of what the questionnaire's caution `detail` actually said -- the same "policy written for one case, applied to a different case" failure mode already fixed for medication/allergy in finding #5, just not extended to these two. | Both modifiers now render the caution's actual `detail` text instead of an assumed specific claim. (Any numbers literally present in that detail text -- e.g. "type 2" -- are now registered as grounded via a small `_numbers_in()` extractor, since they're sourced from real reported data, not narrator invention; without this, embedding the raw detail introduced its own new ungrounded-number failures, caught while fixing this and testing the full suite, not left in.) | Kernel-level; new tests `test_exercise_limitation_modifier_reflects_the_actual_reported_detail`, `test_family_history_modifier_reflects_the_actual_reported_detail`. Full kernel suite (141 tests) re-run clean after this change, including the original sample dataset's real answer text. |
| 9 | Incomplete fix | Medium | Finding #2's conditional-write fix prevents re-entering `RUNNING` from a terminal state, but doesn't prevent overlapping SQS deliveries from both invoking the agent while the record is already `RUNNING` (allowed `RUNNING -> RUNNING`) -- two concurrent deliveries can both call Bedrock. Repeated consumer failures also leave a record stuck `RUNNING` with no reconciliation against messages that end up in the DLQ. | **Not fixed in this pass.** A real fix needs a reclaimable processing lease with attempt ownership (e.g. a fencing token or visibility-timeout-aware claim), plus a reconciliation pass over stale/DLQ'd records -- more than a conditional-write tweak. Documented as a deliberately open backlog item alongside findings #3 and #6. **Closed later** (2026-09-05): `process_job.py`'s `_claim_for_processing` adds a `processing_lease_expires_at` field -- a RUNNING record can only be re-claimed once its lease has actually expired (set a few seconds past the handler's own Lambda timeout, so a still-legitimately-running invocation's lease can never expire out from under it), closing the concurrent-double-processing gap via DynamoDB's own atomic compare-and-swap rather than a fencing token. A new `reconcile_dlq.py`, triggered by the DLQ itself, marks a DLQ'd run's record `FAILED` (conditioned on it still being non-terminal) so it's no longer left stuck forever. See `docs/DECISIONS.md`. | Not independently re-reproduced in this pass beyond reviewing the code path directly (`process_job.py`, `orchestration_stack.py`'s catch wiring) -- taken as credible from the review's own description of the existing conditional-write semantics, which matches this repo's actual code. **Re-verified on close**: new tests reproduce a concurrent delivery arriving while the lease is still valid (rejected, agent never called), a stuck record whose lease has since expired (successfully reclaimed and completed), and all three DLQ-reconciliation outcomes (a stuck `QUEUED` run, a stuck `RUNNING` run, and a race-safety check that an already-`SUCCEEDED` run is never clobbered). Full infra suite (147 tests, up from 140) and `cdk synth` both pass. Redeployed live and re-verified against the real account: a message sent directly to the real SQS queue completed normally (`SUCCEEDED`, real Bedrock answer), and two messages sent for the same run_id within the same second still produced one consistent `SUCCEEDED` result -- CloudWatch's `REPORT` lines showed the second invocation ran only 212ms (versus ~4-4.7s for the two real Bedrock calls), confirming the lease rejected it before it ever reached the agent. DLQ reconciliation itself was not forced live (would require manufacturing real failed deliveries against production) -- see `docs/DECISIONS.md` for the full disposition. |
| 10 | Incomplete fix | Medium | `_start_and_poll_execution`'s async-path success definition (fixed for finding #7 to check the DynamoDB status instead of the raw Step Functions status) still counted `status == "SUCCEEDED"` alone as success, without also requiring `safe is True` -- disagreeing with the sync path's definition, which already required both. A persisted `SUCCEEDED, safe=False` record (finding #1's regression is one concrete way to produce exactly that combination) would be counted as a harness success. | Both async success checks (`_start_and_poll_execution` for Step Functions, `_enqueue_and_poll` for SQS) now require `status == "SUCCEEDED" and safe is True`, matching the sync/adversarial path. | Code-level fix confirmed against the diff; not re-run against the live account in this pass (no new stress-test run was executed here). |
| 11 | Incomplete fix | Low | `run_id_validation.py`'s regex omitted several of AWS's own documented invalid characters for a Step Functions execution name: the surrogate range (`U+D800`-`U+DFFF`) and the two noncharacters (`U+FFFE`, `U+FFFF`). These are reachable via a JSON body's `\uXXXX` escapes even though they can't be typed directly. Normal non-ASCII text (e.g. Chinese characters) correctly remained accepted -- the gap was specifically these code points, not unicode support broadly. | Added `\ud800-\udfff` and the two noncharacters to the disallowed-character class. | Yes -- confirmed directly against the validator: a lone surrogate and both noncharacters are now rejected; normal Chinese-character run_ids and ASCII run_ids remain accepted (both checked, not just the fix in isolation). |
| 12 | Incomplete fix | Medium | Finding #11's IAM narrowing (`grant_write_data`/`grant_put` for `AskHandler`) was real progress but still over-granted: `grant_write_data` includes `dynamodb:DeleteItem` and `dynamodb:BatchWriteItem`, neither of which `adapter.py` calls. The same over-grant pattern (`grant_write_data`/`grant_read_data`/`grant_read_write_data` used as a convenience bucket rather than the exact actions each handler calls) was still present, unaddressed, on every *other* DynamoDB-writing handler. | Every handler's table grant is now an explicit action list matching exactly what its code calls, checked against each handler's source directly (not assumed): `AskHandler`/`EnqueueJobHandler` get `PutItem`+`UpdateItem`; `MarkRunningHandler` gets `PutItem` only; `RecordResultHandler`/`ProcessJobHandler` get `UpdateItem` only; `GetRunHandler` gets `GetItem` only; `CancelRunHandler` gets `GetItem`+`UpdateItem` only. | Full infra test suite (129 tests) and `cdk synth --all` both re-run clean after every grant was narrowed, confirming nothing else in the codebase was relying on a broader permission than it declared. Not re-verified with a live `/ask`/`/runs`/`/jobs` call against the deployed account in this pass (the same kind of live check finding #11 used, not repeated here since no new deploy was made). |
| 13 | Docs | Medium | Several places still overstated what was actually true after round-1's fixes: `README.md` called the deterministic path "provably grounded" (it's grounded by construction, not formally proven, and the runtime check that verifies *any* narrator's output has its own documented limits -- see finding #6 above); `README.md` also framed "the agent never echoes the raw question" as a structural anti-injection guarantee, when `llm_narrator.py` puts the raw question directly into the LLM prompt -- true only for the deterministic narrator's own output construction, not a guarantee about what reaches an LLM narrator as input. `AWS_ROADMAP.md`/`STRESS_TEST.md` still used "guaranteed eventual success" language in a few places the original correction (finding #8) missed. The Bedrock evidence writeup treated a model-level CloudWatch `Invocations` count as independent proof calls came from the Lambdas specifically, when that metric doesn't distinguish caller identity within an account. | All four reworded: the grounded-by-construction distinction, the LLM-input vs. structural-guarantee distinction (with the real mitigation -- downstream safety checks + fallback -- stated explicitly), the remaining "guaranteed" phrases replaced with the measured-capacity wording finding #8 already established, and the CloudWatch claim corrected to name what it actually proves (calls happened) vs. what it doesn't (that they specifically came from Lambda rather than another caller on the same account). | N/A -- documentation-only. |

### Second review: what it said to keep as-is

BM25/SQLite retrieval (parameterized, rejects injection-shaped input,
read-only connection); the S3 evidence bucket's access controls (private,
encrypted -- though the review noted raw question text is stored verbatim
in evidence traces, which is fine for synthetic data but worth minimizing/
expiring before any real personal data is involved, a documentation note
rather than a code fix in this pass); the Cognito configuration (public
client, code flow, PKCE, password policy, default token lifetimes); and
the quality of round-1's own regression tests (meaningful behavioral
tests, not self-referential -- their main weakness was missing adjacent
cases, which several of the tests added in this round now cover).

## Third independent review (reviewed commit `dd85976`, frontend + public hosting)

Scoped explicitly to what rounds 1 and 2 never reviewed at all: the
entire frontend (`frontend/src`) and the same day's earlier Phase 6 work
making the Workbench a public S3+CloudFront-hosted URL, plus targeted
verification of several hotfixes made live during that same work
(async trace persistence's `get_run.py` exception handling, and a
numeric-grounding false-positive fix to `safety.py`). Every finding
below was independently reproduced before being fixed -- via direct
Python reproduction for the two kernel/backend findings, via careful
code reading for the frontend findings (this project has no frontend
test framework), and via a real browser against the live hosted URL for
live verification -- the same standard as rounds 1 and 2.

| # | Severity | Finding | Fix | Live-verified? |
|---|---|---|---|---|
| 1 | High | A live hotfix earlier the same day added a `question_text` exemption to `verify_numeric_grounding`, meant to stop a false positive where the model declined while citing the question's own number. Reopened a genuine bypass: a bare fabricated number matching the question (`"Is my risk score 999?"` -> `"Your... risk score is 999."`) now passed outright, and irregular spacing/Markdown around a real value+unit pair (`"500  mg/dL"`, `"**500** mg/dL"`) made the strict regex miss, falling through to the exempted weak path instead of being checked at all. | Exemption reverted entirely rather than patched further -- reliably distinguishing "declining while citing" from "asserting as fact" isn't solvable with a regex, and a false positive (safe answer replaced by the template) is the acceptable side of the asymmetry, not a false negative (fabricated number reaching the user). | Yes -- reproduced both bypass shapes directly in Python before the revert; both correctly rejected after. Kernel test suite re-run clean (154 tests) after this fix and finding #7's fix together. |
| 2 | Medium | Run history (`localStorage`, full question text) persisted across accounts in a shared browser -- signing out didn't clear it. | `signOut()` now also calls a new `clearHistory()`. | Yes -- `localStorage.getItem('care_agent_run_history')` confirmed `null` in a real browser immediately after clicking Sign Out. |
| 3 | Medium | `AskForm`'s polling used `setInterval(async () => ...)`, which doesn't wait for the previous tick to resolve -- a slower, earlier response can resolve after a faster, later one and overwrite already-terminal state with stale data. | Rewritten as a self-scheduling `setTimeout` (next tick only scheduled once the current one resolves) plus a monotonic generation counter checked before and after each async call, so a superseded poll loop can never write state again. | Yes -- a real Step-Functions run polled cleanly through `RUNNING` to a terminal `SUCCEEDED` state in a live browser against the redeployed backend, no stale overwrite observed. |
| 4 | Medium | A polling error that wasn't tolerated (i.e. not the expected transient 404 right after a Step-Functions start) set an error message but never reset the derived `pending` flag -- permanently disabling submit and mode-switching with nothing actually still running. `handleCancel` also wrote nothing back to the UI until the next poll tick, which might never come. | Added a `pollingStalled` state that overrides `pending`. `handleCancel` now immediately refetches and applies the real post-cancel state instead of waiting on a tick. | Yes -- forced live: a cancel request raced against the run finishing naturally, got a real `409: Run was already finalized`, and the UI correctly displayed the real `SUCCEEDED`/`SAFE` terminal state instead of staying stuck on "Cancelling...". |
| 5 | Medium | `react-markdown` renders `![alt](url)` as a real `<img>` the browser eagerly fetches with no user interaction -- a data-exfiltration vector for text ultimately originating from an LLM completion. | `Markdown.tsx`'s `components` override for `img` now renders a plain `[image: alt -- not rendered]` badge instead, the same override pattern already used for `a`. | No -- verified by code reading and the identical, already-proven link-override pattern; not tested against a live malicious image URL. |
| 6 | Medium | The `AccessDenied`-for-missing-trace fix from earlier the same day only wrapped `get_object()` itself in its `try` block -- reading the response body (`.read()`, which can raise `ReadTimeoutError`, a `BotoCoreError` subclass distinct from `ClientError`) and `json.loads()`-ing it happened after the `except`, unprotected. A transport-level failure mid-stream still 500'd the whole `GET /runs/{run_id}` endpoint. | Both calls moved inside the `try`; now also catches `BotoCoreError`/`ValueError`. | Yes -- redeployed `CareAgentOrchestrationStack` for this fix specifically (before the other findings' fixes existed); new test simulates a `ReadTimeoutError` mid-read and confirms a clean 200 with no `trace` field. |
| 7 | Medium | `mock_narrator.py`'s closing personalization summary hardcoded `"nutrition" in topics or "exercise_volume" in topics` into one combined phrase regardless of which actually fired -- the same overclaim pattern round 2 (finding #8) already fixed for other topics, not extended to this pair. | Split into two independent branches, each naming the specific detail from that modifier's own grounded-fact claim. **A real regression introduced fixing this, caught by the full suite before considering it done**: reusing the `exercise_volume` claim text embedded a literal "60" never registered as grounded, failing numeric_grounding for 10 previously-passing tests -- fixed by registering it via `_numbers_in()`, the exact pattern round 2's finding #8 already established. | Kernel-level; new tests distinguish nutrition-only from exercise-volume-only firing. Full kernel suite (154 tests) re-run clean, including the fix for the self-introduced regression. |
| 8 | Medium | A stored access token was treated as valid regardless of age; an expired token failed every API call with a 401 the app never surfaced, leaving the signed-in Workbench displayed indefinitely. | `completeSignIn` records `Date.now() + expires_in * 1000`; `getAccessToken()` self-clears an expired token; `authedFetch` forces a hard redirect to `/` on any 401 (catches server-side revocation, which client-side expiry bookkeeping alone can't). | Yes, twice -- once forced (an explicitly-expired token blocked a submit with a clean "Not signed in." before any `fetch` call), and once unforced: a real token that aged out over the course of this round's own work hit the live redeployed API, got a genuine 401, and was correctly redirected to a clean sign-in screen rather than getting stuck. |

**A self-inflicted deploy bug, found and fixed during this round's own
live verification, not a review finding**: `cdk deploy --all` without
`CARE_AGENT_WORKBENCH_URL` set (see `app.py`'s documented two-pass
deployment requirement) silently reset `ApiStack`'s CORS `allow_origins`
back to just `localhost:8765`, breaking every API call from the public
CloudFront URL. Caught immediately by testing the redeployed public URL
in a real browser instead of only the local dev server; fixed by
redeploying `CareAgentAuthStack`/`CareAgentApiStack` a second time with
the env var set, confirmed via a live CORS preflight. See
[`DECISIONS.md`](DECISIONS.md) for the full account.

### Third review: what it said to keep as-is

The link-override pattern in `Markdown.tsx` (`target="_blank"
rel="noreferrer"`) as the right template to extend for the image
override; the PKCE/S256 authorization-code flow itself (`auth.ts`) --
the review's session-expiry findings were about tracking an already-
correct flow's token lifetime, not a flaw in the flow; the Origin Access
Control-based S3+CloudFront hosting (no public bucket policy, no
website-hosting endpoint) as the right shape, independently confirmed by
the review directly probing the S3 bucket URL and getting a clean
`AccessDenied`; and the client-side-only run-history design (finding #2
was about clearing it on sign-out, not about the underlying design of
keeping history in `localStorage` rather than a new backend endpoint).
