# Loaded automatically by Vite/Vitest for `mode=test` (vitest's default
# mode). Dummy values only -- no real deployed identifier, so unlike
# .env.local this is fine to commit (see .env.example's own note on why
# real ones aren't).
VITE_COGNITO_DOMAIN=test.auth.us-east-1.amazoncognito.com
VITE_COGNITO_APP_CLIENT_ID=test-client-id
VITE_API_BASE_URL=https://api.test.example.com
